
import socket, strings, random

def random_int(length=100):
	return random.randrange(length)


def spilt_string():
	spilt_string = ""
	printable = string.printable
	len_ss = random_int(len(printable))
	if len_ss <= 50:
		a = '@'
		split
	else:
		a = 
	return spilt_string


def main():
	stringsable = strings.printable